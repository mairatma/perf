{namespace NestedList}

/**
 * @param items
 */
{template .render}
	<div>
		{foreach $item in $items}
			{call NestedListItem.render}
				{param key: 'item' + index($item) /}
				{param text: $item /}
			{/call}
		{/foreach}
	</div>
{/template}
