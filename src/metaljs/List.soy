{namespace List}

/**
 * @param items
 */
{template .render}
	<div class="list">
		{foreach $item in $items}
			<div class="row" key="item{index($item)}">
				<div class="col-md-12">
					<span>{$item}</span>
				</div>
			</div>
		{/foreach}
	</div>
{/template}
