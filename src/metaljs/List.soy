{namespace Templates.List}

/**
 * @param id
 */
{template .render}
	<div id="{$id}" class="component">
		{call .items data="all" /}
	</div>
{/template}

/**
 * @param id
 * @param items
 */
{template .items}
	<div id="{$id}-items">
		{foreach $item in $items}
			<div class="row">
				<div class="col-md-12">
					{call .item}
						{param id: $id /}
						{param surfaceElementId: $id + '-item' + index($item) /}
						{param text: $item /}
					{/call}
				</div>
			</div>
		{/foreach}
	</div>
{/template}

/**
 * @param surfaceElementId
 * @param text
 */
{template .item}
	<span id="{$surfaceElementId}">
		{$text}
	</span>
{/template}
